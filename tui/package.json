{
  "name": "quiver-tui",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Quiver interactive terminal UI (OpenTUI + Solid). Linux-only.",
  "scripts": {
    "dev": "bun run --preload @opentui/solid/preload src/main.tsx",
    "build": "bun run scripts/build.ts",
    "test": "bun test --preload @opentui/solid/preload"
  },
  "dependencies": {
    "@opentui/core": "^0.1.31",
    "@opentui/solid": "^0.1.31",
    "solid-js": "1.9.12"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
